export enum RequestTypes {
  GetState = "getstate",
  ChangeRoom = "changeroom",
  RemoveRoom = "removeroom",
}

export enum PayloadTypes {
  SetState = "Setstate",
  Error = "error",
  ConnInfo = "conninfo",
}
